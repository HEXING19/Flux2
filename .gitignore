__pycache__/
*.py[cod]
*.pyo
*.pyd
.venv/
.pytest_cache/
.mypy_cache/
.coverage*

.env
.env.*

/flux.db
data/*.db

dist/
build/
*.spec
*.tar

*.log
.DS_Store

# Runtime DBs and packaged artifacts are intentionally ignored.
# If similar files were tracked in older history, remove them from the index once
# and keep relying on these rules afterwards.
